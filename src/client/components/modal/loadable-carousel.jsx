import React, { Component } from 'react'
import Loadable from 'react-loadable'

export default class LoadableCarousel extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}